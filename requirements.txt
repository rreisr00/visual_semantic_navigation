# Python dependencies for the visual-semantic navigation project
#
# Install with:
#   pip install -r requirements.txt

# NumPy – must stay on the 1.x ABI for cv_bridge compatibility
numpy>=1.24.0,<2

# Image processing – 4.11+ requires numpy>=2, so cap at 4.10.x
opencv-python>=4.7.0,<4.11

# Deep learning inference
torch>=2.0.0
transformers>=4.45.0
# Pillow is pulled in by transformers but listed explicitly for clarity
Pillow>=9.0.0

# Similarity computation
scipy>=1.10.0

matplotlib>=3.10.8

sentencepiece>=0.2.1

scikit-learn>=1.8.0

protobuf>=7.34.1

chromadb>=0.6.0

ultralytics>=8.0.0

faster-whisper>=1.0

sounddevice>=0.4

# Offline and campaign-analysis notebooks (experiments/{offline,simulation})
pandas>=2.0
networkx>=3.0
nbformat>=5.9
nbclient>=0.9
pyarrow>=14.0
